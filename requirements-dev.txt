# 仅测试/开发环境需要，不随生产部署安装。
# 安装：pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
# httpx 已在 requirements.txt 中（app/services/bsp_client.py 运行时依赖），
# TestClient 用的也是同一份，这里不再重复声明。
